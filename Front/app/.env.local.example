BACKEND_URL=http://host.docker.internal:3001,http://172.17.0.1:3001,http://127.0.0.1:3001
KLIPY_API_KEY=your_klipy_api_key_here
# Set to "true" only when serving over HTTPS (deployed). Leave unset for local HTTP.
SECURE_COOKIE=
