# Components

Reusable UI components for the Next.js app.

Subfolders:

- `home/` : home, servers, chat, friends, WS provider
- `login/` : login form UI
- `register/` : signup UI
- `profile/` : profile UI shell (may be provided by teammate)
