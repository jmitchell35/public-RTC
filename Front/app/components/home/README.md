# Home components

Home, servers, and chat UI.

Key pieces:

- `home-client.tsx` : friends list, requests, add friend
- `home-shell.tsx` : page layout and data wiring
- `home-ws-provider.tsx` : WebSocket connection + event bus
- `chat_client.tsx` : channel chat UI with typing + edits
